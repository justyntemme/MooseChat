MAIN()

START TOR SERVICE() (return onion url)

Start UI() (ask user for connection url)

Take user message and encrypt (option to save pass in mem in config)

send message()

recive message() (decrypt with gpg binary)

present to buffer()

##### Config File --config for config file location

# Onion to connect to 
# GPG key To encrypt for
# GPG key to depcrypt for